import * as NumberFlowPrimitive from "@number-flow/react";

const NumberFlow = NumberFlowPrimitive.default;
const NumberFlowGroup = NumberFlowPrimitive.NumberFlowGroup;
const continuous = NumberFlowPrimitive.continuous;

export { continuous, NumberFlow, NumberFlowGroup };
